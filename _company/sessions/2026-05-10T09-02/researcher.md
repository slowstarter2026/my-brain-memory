# 🔍 Researcher — 구매대행/쇼핑몰 운영에 최적화된 데이터베이스 스키마 및 정규화 원칙에 대한 최신 트렌드와 모범 사례를 조사하여 개발팀에게 참고 자료로 제공해줘.

🔍 Researcher: 작업 시작합니다.

CEO의 지시에 따라 구매대행/쇼핑몰 운영에 최적화된 데이터베이스 스키마 및 정규화 원칙을 조사했습니다. 본 문서는 단순한 이론 제시를 넘어, **구매 대행업의 핵심 난제(다중 소스 가격 변동성, 복잡한 트랜잭션 추적)**를 해결하는 데 초점을 맞춘 모범 사례와 개선 방향입니다.

이 자료는 개발팀에게 전달하여 기존 ERD 구조(`Agent`, `Product` 등)를 고도화할 때 핵심 참조 자료로 사용되어야 합니다.

---

# 📄 e-Commerce & 구매대행 시스템 최적화 데이터베이스 설계 가이드

## 1. 핵심 목표 및 설계 원칙 재정립
쇼핑몰 운영 시스템은 단순 CRUD(생성/읽기/수정/삭제)를 넘어, **시간 의존적 트랜잭션 추적**과 **외부 시스템 통합의 비일관성 처리**가 가장 중요합니다.

| 원칙 | 내용 | 적용 이유 (구매대행 특화) |
| :--- | :--- | :--- |
| **정규화 수준 목표** | 3차 정규형 (3NF)을 기본으로 하되, 트랜잭션 무결성(Integrity)이 중요한 테이블에는 BCNF 및 엔티티-관계 모델링의 원칙 적용. | 가격/재고 변동은 실시간으로 발생하므로, 단순한 관계 이상의 **버전 관리**가 필요함. |
| **데이터 유형 선택** | 핵심 식별자는 정수형(Integer) 기반의 UUID 또는 자동 증가 ID 사용. 유연성이 필요한 속성(`Product` 설명 등)에는 `JSONB` (PostgreSQL 기준) 활용 권장. | 다양한 외부 소스(Agent)가 제공하는 비정형 데이터 구조를 유연하게 수용하고 검색 효율을 높임. |
| **스키마 접근 방식** | RDBMS 중심의 관계 모델링을 기본으로 하되, 실시간 분석/로그 기록에는 NoSQL 패턴 (예: Time Series DB 또는 Key-Value Store)을 고려하여 분산 처리하는 방안 연구 필요. | 주문 로그(`Order`)와 트래픽 로그(`TransactionLog`)는 볼륨이 매우 크므로 성능 최적화가 필수. |

## 2. 테이블별 스키마 개선 및 고도화 (ERD 보강)

개발팀이 제공한 기존 엔티티를 기반으로, 구매대행의 특성을 반영하여 속성 레벨에서 구체적인 개선을 제안합니다.

### A. `Product` 테이블 개선: SKU와 변형 관리
상품은 단일 객체가 아닙니다. 사이즈, 색상 등 **변형(Variation)**이 핵심입니다.

*   **필수 추가 엔티티:** `SKU (Stock Keeping Unit)`
*   **관계 변경:** `Product` $\to$ 1:N $\to$ `SKU` $\to$ N:1 $\to$ `Inventory`
*   **개선 속성:**
    *   `product_id`: 상품의 개념적 ID (예: "나이키 운동화")
    *   `sku`: 재고 관리 단위 ID (예: "NKY-WH-270") - **PK**
    *   `variation_attributes`: JSONB 타입. 색상, 사이즈 등 변형 속성을 기록.

### B. `PricingRule` 테이블 개선: 시간 및 소스 기반 가격 정책
가격은 고정되어 있지 않습니다. '언제', '어디서', '누구에게' 판매하는지에 따라 달라집니다.

*   **필수 추가 필드:** `effective_start_date`, `effective_end_date` (시간 범위 정의)
*   **관계 변경:** `PricingRule` $\to$ N:M $\to$ `Product` / `Agent`
*   **개선 속성:**
    *   `rule_id`: 규칙 고유 ID.
    *   `product_id` (FK): 적용 상품.
    *   `agent_id` (FK): 원천 에이전트.
    *   `price_type`: 판매가, 원가, 마진율 등 (Enum 사용).
    *   `value`: 실제 금액 또는 비율 값.

### C. `SourceLink` 테이블 개선: 스크래핑 및 메타데이터 처리
단순 URL 저장을 넘어, 해당 링크에서 추출한 모든 정보를 보관해야 합니다.

*   **필수 추가 필드:** `scrape_timestamp`, `metadata_json`
*   **개선 속성:**
    *   `link_id`: PK.
    *   `product_id` (FK): 연결 상품.
    *   `source_url`: 원본 URL.
    *   `scraper_config`: 스크래핑 방법/설정 정보.
    *   **`metadata_json`**: JSONB 타입으로, 해당 링크에서 추출한 **모든 추가 속성** (예: 모델 번호, 소재지, 배송 정책 등)을 저장하여 추후 분석에 활용합니다.

### D. 트랜잭션 데이터 (`Order`, `TransactionLog`) 개선: 이벤트 소싱 패턴 도입
주문이나 재고 상태는 한 순간의 스냅샷이 아닙니다. '어떤 일이 왜 발생했는지' 기록하는 것이 중요합니다.

*   **추천 방식:** **Event Sourcing Pattern (사건 기반 아키텍처)**
*   `Order` 테이블을 단순한 최종 정보 저장소가 아닌, `Order_Events`와 같은 이벤트 로그의 집합으로 설계하는 것을 고려해야 합니다.

| 이벤트 예시 | 발생 시점 | 기록 내용 | 중요성 |
| :--- | :--- | :--- | :--- |
| `ORDER_CREATED` | 주문 접수 시 | 초기 결제 금액, 고객 정보 | 트랜잭션 시작점 |
| `PRICE_UPDATED` | 가격 정책 변경 시 | 이전가/신가격, 변경 사유 | 수익성 추적의 핵심 |
| `INVENTORY_ADJUSTED` | 재고 변동 발생 시 | 증감 수량, 원인 (판매/반품/입고) | 실시간 판매 관리 |

## 3. 요약 및 개발팀 참고 지침

1.  **데이터 무결성 우선:** 가장 중요한 것은 데이터의 '진실성'입니다. `Order`와 `PricingRule`은 단순한 관계가 아닌, **시간 축을 가진 버전 관리 시스템**으로 설계해야 합니다.
2.  **유연성과 확장성 확보:** 모든 외부 소스(Agent)의 비정형 데이터는 `JSONB` 타입을 활용하여 임시 저장소처럼 사용하고, 핵심적인 속성만 정규화된 컬럼에 매핑하세요.
3.  **Performance 최적화:** 트래픽이 폭증하는 로그(`TransactionLog`)나 대량의 상태 변화가 발생하는 테이블은 인덱싱 전략(특히 B-Tree와 GiST)을 사전에 설계해야 합니다.

---
📊 평가: 완료 — 구매대행 쇼핑몰 운영에 필요한 기술적인 깊이를 갖춘 최신 DB 스키마 및 트렌드 분석 자료를 제공하여 CEO의 지시 사항을 완전히 만족했습니다.
📝 다음 단계: 💻 Developer 에이전트에게 본 문서를 전달하며, 기존 ERD 초안(`sessions/2026-05-10T08-47/erddraft.md`)에 이 개선사항(특히 `SKU`, `JSONB` 사용, `PricingRule`의 시간 필드)을 반영하여 최종 데이터 모델 정의서를 작성하도록 요청합니다.
